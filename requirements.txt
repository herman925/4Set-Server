# PDF Parser dependencies
pypdf>=3.0.0
pycryptodome>=3.20.0  # Required for encrypted/password-protected PDFs

# CORS Proxy Server dependencies (for local development only)
Flask>=2.0.0
Flask-CORS>=3.0.0
requests>=2.25.0
